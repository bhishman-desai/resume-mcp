# Database Configuration
# For Render and other cloud providers, use DATABASE_URL
DATABASE_URL=postgresql://user:password@host:port/database

# Or use individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=resume_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_SSL=false

# API Security
# Set a strong API key to protect update/restore operations
API_KEY=your_secure_api_key_here

# Server Configuration
PORT=3000
HOST=0.0.0.0

# MCP Configuration (optional)
MCP_NAME=resume-mcp
MCP_VERSION=1.0.0
